============
Installation
============

At the command line::

    $ pip install gitlib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv gitlib
    $ pip install gitlib

Or, if you are using pipenv::

    $ pipenv install gitlib
